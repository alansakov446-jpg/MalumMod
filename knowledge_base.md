# База знаний по Iris | Чат-менеджер (@iris_cm_bot)

Это справочная база для бота-помощника «Ириска». Используй эти данные, чтобы
точно и по делу объяснять пользователю, какую команду Ириса написать и как
именно её оформить. Если вопрос не связан с ботом Iris — вежливо скажи, что
помогаешь только по Iris | Чат-менеджеру.

## Общие правила синтаксиса

- Префиксы команд: `!`, `.`, `/`, а также слова «Ирис» и «Ириска». Многие
  команды работают вообще без префикса.
- `{значение}` — обязательный параметр, подставляется без скобок.
- `[вариант1|вариант2]` — нужно выбрать один из перечисленных вариантов.
- `{период}` — число + единица времени: минута/час/сутки/неделя/месяц/год.
  Например: `2 дня`, `1 неделя`, `10 минут`.
- `{ссылка}` — @username, ссылка на профиль (t.me/username) или ответ (reply)
  на сообщение нужного пользователя. Узнать ID: `!ид {ссылка}`.
- Причину наказания (бана/мута/варна) принято писать на следующей строке,
  после самой команды.
- `{enter}` — перенос строки в описании команды.
- Ранги модерации: 0 — участник, 1 — младший модератор, 2 — старший модератор,
  3 — младший администратор, 4 — старший администратор, 5 — создатель.
  Вышестоящий по рангу может отменять решения нижестоящих (но не наоборот).

---

## 1. Модерация: ранги, назначения

- Назначить модератора: `+Модер {ссылка}` — 1 ранг. `+Модер 2 {ссылка}` — 2
  ранг, и т.д. до 5. Синонимы: `+админ {ссылка}`, `Повысить {ссылка}` (на 1
  ранг выше), а также запись через восклицательные знаки: `!модер` (1 ранг),
  `!!модер` (2 ранг), `!!!модер` (3 ранг), `!!!!модер` (4 ранг).
- Понизить на 1 ранг: `Понизить {ссылка}`.
- Снять модератора: `Снять {ссылка}` / `Разжаловать {ссылка}`. Снять всех
  вышедших модераторов: `Снять вышедших`. Снять вообще всех модераторов
  чата, включая создателя: `!Снять всех` / `!Разжаловать всех`.
- Назначить/разжаловать модератора сразу во всей сетке чатов:
  `Сетка {число «!»} модер {ссылка}` (количество «!» = ранг), снять —
  `Сетка разжаловать {ссылка}`.
- Снять с себя полномочия модератора: `!Снимаю полномочия` (синонимы:
  `!ухожу в отставку`, `!разжаловать меня`); во всей сетке —
  `Сетка ухожу в отставку`.
- Восстановить права создателя (5 ранг), если снял их сам: `Восстановить
  создателя` (синонимы: `!восстановить владельца`, `!хв`); во всей сетке —
  `Сетка восстановить создателя`. Работает только для реального создателя
  чата.
- Завещание — передача доступа к аккаунту доверенному лицу на случай утери:
  `+Завещание {ссылка}` (синоним `+наследство`), посмотреть — `Моё
  завещание`, отменить — `-Завещание`. Вступить в наследство:
  `Вступить в наследство {ссылка}`.
- Передать права настоящего создателя другому пользователю насовсем:
  `!Передать создателя` (нельзя отменить самостоятельно, получателю нужно
  ввести `Восстановить создателя`).
- Кто модерация чата: `Кто админ` (синонимы: `а судьи кто`, `кто здесь
  власть`, `!staff`, `!админы`). Кто назначил пользователя:
  `Кто назначил {ссылка}` (синоним `!роль {ссылка}`).
- Лог изменений рангов: `Модер лог`, `Твой модер лог {ссылка}`,
  `Мой модер лог`, `Модер лог от {ссылка}`.
- Созвать модерацию (до 20 человек): `Созвать модеров` (синонимы:
  `позвать админов`, `позвать модеров`).
- `+Модер теги` / `-Модер теги` — включает/выключает отображение ID
  модератора и типа наказания в ответах бота.

## 2. Баны, муты, кики, предупреждения

- **Мут**: `Мут {срок} {ссылка}` (по умолчанию 1 неделя), причину — на
  следующей строке. Синонимы: `заткнуть`, `mute`, `Мут остыть` (10 минут).
  Снять: `-Мут {ссылка}` (синонимы: `снять мут`, `говори`, `unmute`,
  `размут`). Список замученных: `Муты`. Проверить: `Проверить мут {ссылка}`.
  Срок по умолчанию: `Мут период {период}` (минимум 1 минута).
- **Бан**: `Бан {период} {ссылка}` (без периода — навсегда). Синонимы: `чс`,
  `!ban`, `!permban`, `Бан остыть` (10 минут). Причину — на следующей
  строке; желательно ставить «Спам»/«Рейдер» и т.п. для базы антиспама.
  Срок по умолчанию: `Бан период {период}`.
  Разбан: `Разбан {ссылка}` / `Вернуть {ссылка}` (синоним `!unban`).
  Причина и кто забанил: `Причина {ссылка}`. Список банов: `Банлист`.
  Снять все баны в чате: `!Амнистия`.
- **Кик**: `Кик {ссылка}` (синоним `!kick`) — не мешает вернуться. Тихий
  кик без уведомления: `Кик тихо {ссылка}`.
- **Предупреждения (варны)**: `Варн {период} {ссылка}` (синонимы: `пред`,
  `предупреждение`, `/warn`, `!warn`, `варн остыть` — 10 минут). Причину —
  на следующей строке.
  Снять последнее: `-Варн {ссылка}`. Снять определённое число:
  `Снять варны {число} {ссылка}`. Снять варн под номером:
  `Снять варн номер {число} {ссылка}`. Снять все: `Снять все варны {ссылка}`.
  Список варнов пользователя: `Варны {ссылка}`, свои — `Мои варны`
  (синоним `мои преды`). Список последних варнов чата: `Варнлист`.
  Лимит, после которого кикает: `Варны лимит {число}` (по умолчанию 3).
  Срок бана после лимита: `Варны чс {период}`. Срок хранения варна:
  `Варны период {период}`.
- Массовая чистка участников: `Кик неактив {период}` (минимум 24 часа,
  либо `Кик неактив {число}` — самых неактивных); `Кик актив` (актив за
  последние ~10 минут, полезно при рейде); `Кик новичков {период}`
  (недавно вступившие — против рейдов); `Кто удалён` / `Кик удалённых`
  (синонимы: `кто собака` / `кик собак`); `Кик молчунов {дней}` (состоят
  давно, но ничего не написали); `Кик по смс {число сообщений} {период}`
  (синоним `кик по сообщениям`, по умолчанию период — неделя).

## 3. Триггеры (автоматизация)

Триггер — правило: «на событие X бот автоматически выполняет команду Y (или
несколько, до 3)». Так настраиваются автонаказания, смена исхода дуэлей/
кубов/рулетки, реакции на спам-паттерны и т.д.

Сейчас доступны события: `русская рулетка`, `дуэль`, `кубы`, `маты`,
`варнлимит` (превышение лимита предупреждений), `ссылки`, `стикеры`, `капс`,
`гостевые боты`, `гс` (голосовые/видеосообщения).
Команды-исходы, которые можно вписать в триггер: `варн`, `мут`, `бан`, `кик`.

Синтаксис установки:
```
+триггер {событие} {приоритет}
{Команда} / [причина]
```
Приоритет `{1-5}` — от чьего ранга модератора выполняется автонаказание
(по умолчанию 0). Причину указывают только там, где она уместна (варн, бан,
мут). Можно добавить до 3 команд подряд в одном триггере.

Примеры:
```
+триггер дуэль 1
Мут 10 минут / Ты проиграл, отдохни 10 минут
```
```
+триггер варнлимит 1
Мут час / Лимит варнов!
```
```
+триггер стикеры 0
Варн / Отправлять стикеры запрещено!
```

Просмотр: `Триггер {событие}` (подробности), `Триггеры` (список, синоним
`триги`). Удалить: `-Триггер {название}`. Ограничить доступ к настройке
триггеров: `Дк унб {ранг}` (по умолчанию 4 ранг).

Связанные фильтры для автонаказаний:
- Мат: `-Маты` (включить фильтр) / `+Маты` (выключить).
- Голосовые/видеосообщения: `+Гс` / `-Гс`.
- Стикеры: `-Стикеры {количество}` (лимит, по умолчанию 1 = запрет
  полностью), снять — `+Стикеры`.
- КАПС: `-Капс {% капса} {мин. длина сообщения}` (по умолчанию 80% и 5
  символов), снять — `+Капс`.
- Гостевые боты: `+Гостевые боты` / `-Гостевые боты` (разрешить/запретить
  в принципе), точечно — `+Гостевой бот {ссылка}` / `-Гостевой бот
  {ссылка}` (синоним `+гостьбот` / `-гостьбот`).
- Флуд реакциями — можно ограничить количество реакций за 10 секунд и
  наказывать нарушителей через триггер.

Подробный гайд: t.me/irissup, teletype.in/@iris_cm/triggers. В чате также
можно вызвать шпаргалку командой `Триггер помощь`.

## 4. Настройка доступа команд (ДК)

- Посмотреть доступность всех команд: `Доступ команд` (синоним `!дк`).
  Только свой доступ: `Мой доступ команд` (синоним `!мдк`).
- Сделать команду доступной всем: `Дк {название/номер} 0` (синоним
  `+дк {название/номер}`).
- Ограничить по рангу: `Дк {название/номер} {ранг}`, например `!дк бан 2`.
- Полностью отключить команду: `Дк {название/номер} 6` (синоним
  `-дк {название/номер}`).
- Скопировать всю настройку доступа из другого чата (нужен 5 ранг в обоих):
  `Импорт команд из {код чата}`.
- Сброс всех настроек доступа: `!Сброс команд`. Сброс конкретного раздела:
  `!Сброс дк {номер раздела}`.
- Оповещения о (не)доступности команд: `+Команды` / `-Команды`.
- Личный доступ конкретному пользователю (исключение из общих правил):
  `+Лдк {команда/раздел} {ссылка}` / `-Лдк {команда/раздел} {ссылка}`.
  Просмотр: `Лдк {ссылка}` (что разрешено/запрещено пользователю),
  `Все лдк`, `Лдк {команда/раздел}` (кому именно открыт доступ). Сброс:
  `!Сброс всех лдк`, `!Сброс лдк {ссылка}`.
- Редактирование ДК через л/с бота: `+Дк в лс` / `-Дк в лс` (закрывается
  автоматически через час; настраивает только создатель).
- Включить/выключить целиком РП: `+Рп` / `-Рп`; браки: `+Браки` / `-Браки`.
- Лог изменений доступа: `Лог дк`, `Лог дк {название/номер}`,
  `Лог дк от {ссылка}`.

Раздел с подпунктами ограничивается через главную команду-блок — узнать её
название можно из `Доступ команд`.

## 5. Чистка чата

- Удалить одно сообщение (ответом): `-Смс`.
- Удалить N сообщений над командой: `-Смс {число}`, тихо (без уведомления):
  `-Смс {число} тихо`.
- Удалить сообщения ниже отмеченного (строго ответом на сообщение):
  `!Пург {число}` или `!Пург` (всё ниже), тихо — `!Пург тихо`.
- Лимиты: обычный чат — до 100 сообщений раз в 3 минуты; премиум-чат — до
  1000 раз в минуту. Нельзя удалять сообщения старше 48 часов; чистка
  больших объёмов может занять до ~20 минут.
- Кто вернулся после чистки: участники всё равно смогут зайти обратно по
  ссылке-приглашению.

(Массовые кики неактивных/новичков/удалённых — см. раздел 2 выше.)

## 6. Настройка чата

- Правила: `+Правила` (текст на следующей строке), показать — `Правила`.
- Приветствие: `+Приветствие` (текст на следующей строке). Переменные:
  `{имя}` — имя и фамилия; `{ж|м|мн}` — форма текста по полу/числу.
- Закреп (ответом на сообщение): `!Закреп` (или по ID: `!Закреп {ID}`,
  ID узнать командой `Смс ид`). Синонимы: `!пин`, `!pin`. Открепить:
  `!Открепить` / `!Открепить {ID}` (синонимы `!анпин`, `!unpin`).
- Название чата: `!Название {текст}`. Название темы (топика):
  `!Топик название {текст}`. Описание чата: `+Описание чата {enter} {текст}`
  / удалить `-Описание чата`.
- Чат-ссылка: создать — `+Чат ссылка`, по заявкам — `+Чат ссылка по
  заявкам`; удалить — `-Чат ссылка`; сбросить все прежние ссылки —
  `!Сброс ссылок`; показать — `Чат-ссылка`.
- Открыть/закрыть чат для обычных участников: `+Чат` / `-Чат` (только
  админы смогут писать). Аналогично для конкретной темы: `+Топик` /
  `-Топик`.
- Разрешения ТГ-участников: `Тг разрешения чата` (панель для всех),
  `Тг разрешения {ссылка}` (для одного участника).
- ТГ-администраторы: `+Тг админ {должность} {ссылка/ответ}` (даёт право
  банить/добавлять участников, удалять и закреплять сообщения). Снять:
  `-Тг админ {ссылка}`. Панель прав: `Тг права {ссылка}`, по всей сетке —
  `Сетка тг права {ссылка}`. В сетке разом: `Сетка +тг админ {должность}
  {ссылка}` / `Сетка -тг админ {ссылка}`. Максимум 50 админов в чате.
- Тег участника (приписка у имени, до 16 символов): `+Тг тег {текст}
  {ссылка}` / удалить `-Тг тег {ссылка}`.
- Сообщения от имени каналов: `+Каналы` / `-Каналы` (разрешить/запретить).
- Уведомления о входах/выходах: `+Входы` / `-Входы`, `+Выходы` / `-Выходы`,
  разом — `+Входы-выходы` / `-Входы-выходы`. Порог активности для
  уведомления о выходе: `+Выходы {число}` (мин. сообщений у пользователя).
- Автокик: `+Автокик` (кикать после самостоятельного выхода — на случай
  повторных входов), с параметрами `Автокик {число} {время} {кик/бан}`
  (например «автокик 3 60 бан»). Отключить — `-Автокик`. Автокик молчунов
  через срок: `+Автокик молчунов {срок}` / выключить `-Автокик молчунов`.
- Минимальный возраст аккаунта во вселенной Ириса для вступающих:
  `+Минрег {число дней}` / отключить `-Минрег` / посмотреть `Минрег`.
- Заявки на вступление: автопринятие — `+Автозаявки` / выключить
  `-Автозаявки` (не примет пользователей из базы Ирис-антиспам).
- Проверка присутствия анкет в чате (в т.ч. для очистки статистики от
  вышедших): `Проверить в чате`.
- Оповещения о нажатиях инлайн-кнопок: `+Инлайны` / `-Инлайны`.
- Обновить информацию о чате (число участников, текущий создатель):
  `Обновить чат`.
- Код чата (для привязки к л/с, переезда, команд из л/с): `Код чата`
  (синоним `код беседы`). ID чата в Telegram: `!Чат ид`. ID сообщества:
  `!Сообщество ид`.
- Привязка к л/с бота: в чате — `!Привязать`; в л/с бота —
  `!Привязать {код чата}`. После привязки в л/с доступны: настройки,
  наказания, ДК, модерация, профиль, закреп, чистка смс, статистика, общий
  сбор, РП-команды, анонимки, действия в отношениях.
- Свёрнутые списки: `+Короткие списки` / `-Короткие списки`.
- ID сообщения (ответом на него): `Смс ид`. Прямая ссылка на сообщение по
  ID: `Перейти к смс {ид}`. Ссылка на ветку реплаев (ответом на смс):
  `!Ветка`.

### Безопасность

- Лимит одновременных приглашений одним участником: `Инвайты {число}`
  (0 — снять ограничение).
- Антирейд: `Антирейд {число}` — лимит попыток пригласить забаненного,
  после чего пригласивший тоже банится; `+Антирейд` (синоним `+чп`) — лимит
  сразу 1 (бан после первой же попытки); выключить — `-Антирейд`
  (синонимы `-чп`, `антирейд 0`).
- Ссылки: `+Чаты` / `-Чаты` (ссылки на частные чаты), `+Сайты` / `-Сайты`
  (внешние сайты), `+Теги` / `-Теги` (ресурсы Telegram), `+Ссылки` /
  `-Ссылки` (все виды разом), `+Боты` / `-Боты` (приглашение ботов в чат).
  Исключения: `!Разрешить ссылку {ссылка}` / `!Удалить ссылку {ссылка}`.

## 7. Сетка чатов

Объединяет несколько своих чатов для управления одной командой.

- Список чатов сетки: `Чаты`.
- Глобальный модератор/администратор: `+Глмодер {ссылка}` / `-Глмодер
  {ссылка}` (выдаёт гладмин или создатель сетки); `+Гладмин {ссылка}` /
  `-Гладмин {ссылка}` (только владелец сетки). Список: `Сетка модеры`.
  Назначить модератора нужного ранга во всех чатах: `Сетка {число «!»}
  модер`.
- Глобальный бан/разбан: `Глобан {ссылка}` / `Глоразбан {ссылка}`. Список
  забаненных в сетке: `Сетка баны`. Кик из всех чатов сетки:
  `Сетка кик {ссылка}`.
- Доступ команд по всей сетке: `Сетка +дк {номер/название}` / `Сетка -дк
  {номер/название}` / `Сетка дк {номер/название} {ранг}`.
- Права в сетке: `Сетка снимаю полномочия`, `Сетка восстановить
  создателя`, `Сетка передать создателя {ссылка}`.

## 8. Анкета, профиль, статистика

- Своя/чужая анкета: `Моя анкета`, `Анкета {ссылка}`; видимость —
  `+Анкета` / `-Анкета`.
- Настройки анкеты: `Мой пол {м|ж|др}` (удалить `-Мой пол`); `!Мой город
  {название}` (удалить `-Мой город`); `Мой др {дд.мм.гг} [всё|месяц|год]`
  (удалить `-Мой др`).
- Карточка пользователя: `Кто я` (синонимы `профиль`, `хто я`), `Кто ты
  {ссылка}` (синоним `Профиль {ссылка}`). График активности: `Моя стата`.
- Описание «о себе»: `О себе {enter} {текст}` (синоним `Описание {enter}
  {текст}`, лимит ~3800 символов), удалить `-О себе`, чужое — `Описание
  {ссылка}`.
- Ник в чате: `+Ник {текст}` (лимит 30 симв., синоним `!ник`), показать —
  `Ник` / `Ник {ссылка}`, удалить — `Ник удалить` (синоним `-ник`).
- Звание: `+Звание {текст}` (лимит 30 симв.), показать — `Звание` /
  `Звание {ссылка}`, удалить — `Звание удалить` (синоним `-звание`).
- Девиз (в рейтингах, лимит 100 символов): `+Девиз {текст}`, удалить
  `-Девиз`, показать `!Девиз`.
- Гражданство (отмечает «жителя» чата, значок 🏡 в статистике):
  `+Гражданство`. Список граждан: `Все граждане` (синонимы `кто гражданин`,
  `кто граждане`).
- Ачивки: `Мои ачивки`, видимость для других — `+Ачивки` / `-Ачивки`,
  чужие — `Твои ачивки` (синоним `покажи ачивки`).
- Подписки (максимум 50): `+Подписка` / `-Подписка {ссылка}`. Созвать
  подписчиков: `Созвать своих` (синоним `позвать своих`; без VIP — до 20
  человек, с VIP — до 120). Топ инфлюенсеров: `Все сабы` (по вселенной),
  `Сабы чата`. Списки: `Мои подписки`, `Мои сабы`, `Подписки {ссылка}`.
- Награды пользователя: `Награды {ссылка}`. Дата регистрации во вселенной
  Ириса: `!Регистрация {ссылка}` (синоним `!рег {ссылка}`). ID: `!Ид
  {ссылка}` (синоним `!id`).

### Статистика

- Активность семейства Ирис-ботов: `!Актив Ириса`.
- График активности в профиле/чат-инфо: `+Стата` / `-Стата`.
- Общая инфа о чате: `Чат инфо`. Картинка со статистикой: `Чат стата`,
  за период (30–5000 дней, по умолчанию 365): `Чат стата {число дней}`.
- Почасовая активность: `Стата по часам` (своя) / `Стата по часам
  {ссылка}` (чужая); почасовая по чату — `Чат стата по часам`.
- Топ по сообщениям: `Стата сутки` (синонимы `Стата`, `Топ`, `Топ сутки`),
  `Стата неделя` (синоним `Топ неделя`), `Стата месяц` (синоним `Топ
  месяц`), `Стата вся` (синоним `Топ вся`). Число участников в топе (до
  100): `Топ {число} {период}`, например `Топ 10 неделя`.
- Вложения ирис-коинов в чат: `Бтоп стата` (история), `Бкоин {число}`
  (зачислить коины в баланс чата).
- Старожилы: `Олды` (🔥 — активны за последний месяц). Новички: `Новички`.
- Кто онлайн: `Кто онлайн` (синоним `Кто в сети`). VIP/не VIP в чате:
  `Кто вип` / `Кто не вип`.
- Список ников чата: `Ники` (постранично `Ники {номер}`), сброс —
  `!Сброс ников`.
- Число участников: `!Население`. Список банов пользователя по всем чатам:
  `Мои баны` (синоним `Мой спам`, использовать в л/с бота). Кто добавил в
  чат: `Кто меня добавил` (себя), `Кто добавил {ссылка}` (другого). Список
  своих чатов: `Мой актив` (в л/с бота, максимум 20).
- Неактивные/молчуны: `Список неактив {период}` (синоним `Кто неактив
  {период}`), `Список молчунов {период}` (синоним `Кто молчун {период}`),
  `Список по смс {кол-во} {период}`.
- Позвать молчунов: `Позвать молчунов` (синоним `созвать молчунов`).
  Позвать конкретных: `Позвать {enter} {ссылки}` или ответом на сообщение
  со списком — `Позвать`. Упомянуть всех: `Созвать всех {enter} {текст}`
  (синоним `Общий сбор`); только онлайн: `Созвать онлайн {enter} {текст}`.

## 9. Темы и оформление модерации

- Названия рангов модерации: `Модераторы названия` (посмотреть текущие),
  список готовых тем — `!Темы`, применить — `!Темы {номер/тема}`.
  Свои названия задаются построчно командой `Модераторы названия` с
  указанием падежей (и=именительный, р=родительный, д=дательный,
  т=творительный, мн=множественное число) для каждого ранга 0–5.
- Иконка модерации (вместо ⭐️, премиум-чаты): `+Иконка модераторов
  {эмодзи}` / вернуть по умолчанию `-Иконка модераторов` (синоним `+Админ
  иконка {эмодзи}` / `-Админ иконка`).

## 10. Голосования

- Голосование за произвольную команду: `+Гк {число голосов} [ранг]
  {команда}` (по завершении команда выполнится от имени организатора,
  если у него хватает прав). Отменить: `-Гк {номер голосования}`.
- Голосование за бан (ГБ): `Гб {ссылка}` (по умолчанию 5 голосов, ранг 0)
  или с параметрами `Гб {число голосов} {ранг} {ссылка}`. Остановить:
  `Гб стоп {ссылка}`. Инфо: `Гб инфо {ссылка}`. Список активных: `Гб
  список`.

## 11. Ирис-антиспам и SCAM-база

- Ирис-антиспам — база пользователей, которых Ирис ранее банил за спам-
  рассылки (ссылки на чаты/каналы/ботов); при включённом фильтре таким
  пользователям запрещён вход. Включить/выключить: `+Ирис антиспам` /
  `-Ирис антиспам` (синонимы `+Ирис спам` / `-Ирис спам`).
- SCAM-база — база мошенников (кинувших на ириски/валюту); такие
  пользователи могут заходить и пользоваться ботом, но получают пометку
  🆘 и часть ограничений.

## 12. Ириски, чеки, бонусы, VIP

- Ириски 🍬 — главная валюта, не привязана к чату. Купить: `Купить
  {число}` (продолжение в л/с бота). Передать: `Передать {число}
  {ссылка}`. Без очков доната (комиссия снимается с ирисок): `Передать без
  од {число} {ссылка}`. С ручным числом очков доната: `Передать {число} од
  {кол-во од} {ссылка}`.
- **Мешок** — это ваш личный кошелёк/баланс: ириски, звёзды, Ирис-голд,
  очки доната и т.д. Никаких «сундуков», «поисков предметов» или лутбоксов
  там нет — это чисто цифры баланса. Посмотреть свой баланс: `Мешок`.
  Показать баланс собеседнику (реплаем): `Мешок {ссылка}`. По умолчанию
  мешок виден только владельцу; сделать его публичным (обязательно для
  участия в «Золотом рейтинге» самых богатых участников): `+Мешок`,
  скрыть обратно — `-Мешок`. Ограничить доступ к команде для чата:
  `Дк мешок {ранг}` (синонимы `+дк мешок` / `-дк мешок`).
  Звёзды бывают двух видов и хранятся раздельно: купленные лично — в
  мешке (команда `Мешок`), подаренные другими — в репутации (раздел
  «20. Репутация»); это разные счётчики.
- Запрет/разрешение на перевод вам валюты (ирисок, ОД, голда): `Запретить
  переводы {ссылка}` / `Разрешить переводы {ссылка}`, для всех сразу —
  `Запретить переводы всем` / `Разрешить переводы всем`.
- Чеки — резервирование и передача суммы по коду. Создать: `Создать чек
  {сумма}` (синоним `+чек`). Список своих: `Мои чеки`. Инфо: `Чек
  {номер/код}`. Отменить: `Отменить чек {номер/код}` (синонимы `удалить
  чек`, `-чек`). Активировать: `Активировать чек {код}`.
- Именной чек (привязан к конкретному получателю, защищённая сделка):
  `+Именной чек {сумма} {ссылка}`. Активируется один раз только указанным
  пользователем; самостоятельно отменить нельзя (только через поддержку,
  комиссия 15%).
- История ирисок: `Где мои ириски` (синонимы `куда пропали мои ириски`,
  `!история ирисок`).
- Безопасные переводы (предупреждение при первом переводе новому
  пользователю): меню — `!Безопасные переводы`; порог по звёздности —
  `!Безопасные переводы зв {число}`; порог по дате регистрации —
  `!Безопасные переводы рег {дата}`; выключить целиком — `-Безопасные
  переводы`. Обойти разово — двойной префикс: `!!передать {число} @`.
- VIP-статус — доп. функции (эмодзи в нике, свои РП-команды и т.д.).
  Модератор чата может назначить свою цену покупки VIP (10–10000 ирисок).
- Список прочих бонусов: Вип, Банхаммер (бан на сутки), Плюсопад
  (увеличить лимит плюсов), Минусит (выдача минусов), Коины (репутация/
  пиар чатов), Анонимка (анонимное сообщение в чат), Анонимка РП,
  Telegram-бонусы (Premium/подарки/звёзды за валюту Ириса).

## 13. Развлечения

- Пинг бота: `Пинг` / `Кинг` / `Пиу` / `Бот` (ответит «Понг»/«Конг»/«Пау»/
  «На месте»).
- Случайное число: `Рандом {число1} {число2}` (без второго параметра —
  диапазон 0…число1).
- Да/нет/неопределённо: `!Данет {вопрос}`. Выбор из двух: `!Выбери
  {первое} или {второе}`. Случайный % шанса: `!Инфа {текст}`. Жребий среди
  ссылок: `!Жребий {ссылки}`. «Кто...»: `!Кто {текст}`.
- Русская рулетка: `!Русская рулетка` (синонимы `ирис рулетка`,
  `!рулетка`) — по умолчанию проигравшего кикает, исход настраивается
  триггером.
- Погода: `!Погода {город}` (прогноз на 5 дней), без города — берёт город
  из анкеты.
- Шипперинг: `Шипперим` (случайные пользователи) / `Шипперим {ссылка1}
  {ссылка2}` (конкретные). Запретить шипперить себя: `-Шип меня` /
  разрешить `+Шип меня`. Списки: `Пейринг` (в чате), `Общий пейринг`
  (по вселенной). Сброс своих пар: `!Сбросить пейринг`.
- Повторить текст от лица бота: `!Скажи {enter} {текст}`.
- Ферма (бесплатные ирис-коины, раз в 4 часа): `Ферма` (синонимы `!бизнес`,
  `фарма`, `фармить`, `!работа`, `!работать`).
- РП-команды (пример: `Пожать руку {ссылка}`) — большой набор ролевых
  действий; доступность по возрастным категориям чата: 0+, 13+, 18+
  (настраивают старшие модераторы командой вида `рп доступ к 18+`).

## 14. Дуэли

- Вызвать на бой: `Дуэль {ссылка}`. Предложить любому желающему: `Кто
  дуэль`. Принять/отклонить: `Дуэль да` / `Дуэль нет`. Отменить свой вызов:
  `Дуэль отмена`. Стрелять: `Выстрел`. Прицелиться (+шанс попадания
  следующего выстрела): `Прицелиться`. Сбросить бонус прицела: `Сбросить
  прицел`.
- Исход при проигрыше (по умолчанию — кик из чата), настраивается:
  `Дуэли исход 0` (ничего), `Дуэли исход кик`, `Дуэли исход бан минута`,
  `Дуэли исход бан 10 минут`, `Дуэли исход бан час`, `Дуэли исход бан
  сутки`, `Дуэли исход бан навсегда`. Также можно настроить через триггер
  на событие `дуэль`.
- Статистика: `Дуэли стата` (победы/ничьи/поражения). Сброс: `!Дуэли
  сброс` (синоним `!сброс дуэлей`).
- Золотая дуэль — на ставку в Ирис-голд: `Дуэль {ставка} {ссылка}`,
  победитель получает удвоенную ставку. Рейтинг: `Золотые дуэли стата`.

## 15. Кубы

- Предложить конкретному: `Кубы {ссылка}`. Любому желающему: `Кто кубы`.
  Согласиться/отказаться: `Кубы да` / `Кубы нет`. Отменить: `Кубы отмена`.
  Статистика чата: `Кубы стата`.
- Исход при проигрыше: `Кубы исход бан` / `Кубы исход кик`, снять —
  `Кубы исход 0`; либо через триггер на событие `кубы`.
- Золотые кубы (на ставку Ирис-голд, ничья — ставка возвращается):
  `Кубы {ставка} {ссылка}` или `Кто кубы {ставка}`.

## 16. Кланы

Кланы — группировки внутри чата с неограниченным числом участников. Можно
состоять только в одном клане одновременно (вступление в другой — выход из
прежнего).

- Создать: `Создать клан {название}` (описание — со следующей строки).
  Изменить: `Клан изменить {название}`. Инфо: `Мой клан`. Удалить свой:
  `Удалить клан`. Модератору удалить чужой по номеру: `!Удалить клан
  {номер}`.
- Вступить: `Клан вступить {номер}` (синоним `+клан {номер}`). Выйти:
  `Выйти из клана` (синоним `-клан`).
- Список: `Кланы` (постранично `Кланы {номер страницы}`).
- Поднять репутацию клана (выше место в списках): `Клан коины {число}`.

## 17. Кружки

Аналог кланов, но по интересам: можно состоять сразу в нескольких кружках.

- Создать: `Создать кружок {название}` (описание — со следующей строки).
  Изменить: `Кружок {номер} изменить {название}`. Сделать основным (отметка
  `*` в «Моих кружках»): `Кружок основа {номер}`. Удалить: `Удалить кружок
  {номер}`.
- Вступить: `Кружок вступить {номер}` (синоним `+кружок {номер}`). Выйти:
  `Выйти из кружка {номер}` (синоним `-кружок {номер}`).
- Список всех: `Кружки` (постранично). Мои: `Мои кружки`. Участники
  конкретного: `Участники кружка {номер}`.
- Поднять репутацию: `Кружок {номер} коины {число}`.

## 18. Отношения

Интерактивная система парных действий, более 13 уровней близости (нужно
копить опыт). По умолчанию команды уходят в л/с бота; чтобы вывести в чат —
добавить суффикс «в чат» или «!» (например `отн стата в чат @id`).

- Создать/восстановить: `+Отн {ссылка}`. Разорвать/отменить заявку: `-Отн
  {ссылка}` (синонимы `отн разорвать`, `отн расстаться`). Принять: `Отн
  да`. Отклонить: `Отн нет`. После расставания 30 дней есть на
  восстановление (крепость падает на 5–50% в зависимости от срока разлуки).
- Основные отношения (чтобы не указывать ссылку каждый раз): `Отн основа
  {ссылка}`. Текущий статус: `Статус отн {ссылка}` (синонимы `отн статус`,
  `статус отношений`).
- История действий: `История отн {ссылка}` (синоним `история отношений`).
  Список всех своих: `Мои отношения` (синоним `мои отн`).
- Список отношений чата: `Отны` (постранично). Топ по вселенной: `Топ
  отнов`.
- Статистика конкретной пары: `Отн стата {ссылка}`. Подарки: `Отн подарки`.
  Доступные действия: `Отн действия {ссылка}` (✅ доступно, 🕔 откат, 🔓
  безлимит, ☢️ платно коинами).
- HP отношений (падает каждый час быстрее, если партнёров нет в чате) —
  информационный показатель, отдельной команды на изменение нет.

## 19. Браки

Зарегистрированные отношения; после развода союз можно восстановить в
течение 3 дней без потери накопленного стажа.

- Предложить/восстановить: `Брак {ссылка}`. Принять: `Брак да`. Отклонить:
  `Брак нет`. Развестись: `!Развод`.
- Модератору: поженить пару — `Поженить пару {ссылка} {ссылка}`, развести —
  `Развести пару {ссылка} {ссылка}`.
- Инфо: `Мой брак`, `Твой брак {ссылка}`. Список браков чата: `Браки`
  (постранично).
- Сброс всех браков чата: `!Сброс браков`. Расторгнуть браки вышедших:
  `Развести вышедших`.
- Продление: цена — `Брак цена продления {число}`, продлить —
  `Брак продлить {кол-во дней}` (синоним `продлить брак {кол-во дней}`).
- Топ самых долгих браков: `Топ браков` (постранично). Участвовать в
  рейтинге: `+Брак рейтинг` / исключить — `-Брак рейтинг`.
- Авторазвод при выходе из чата: `Брак режим развода выключить` /
  `Брак режим развода один` (один из супругов вышел) / `Брак режим
  развода оба` (вышли оба).

## 20. Репутация

Поднимается «плюсами» к пересланному сообщению: `+`, `++`, `+++` и т.д.
(синонимы отправки плюса: лайк, респект, F, уважение, жиза, плюс,
красавчик, справедливо). Налоги: на плюсы — минус 5% каждые 3 дня; на
коины — минус 1% каждые 2 дня.

- Таблица рейтинга: `Рейтинг`. Плюс/минус к сообщению: `+{число}` /
  `-{число}`. Звёздный плюс: `*{число}` (синоним `^{число}`). Повысить
  свою звёздность до значения: `!Повысить звёздность до {число}`. Сброс
  всего рейтинга чата: `!Сбросить рейтинг` (синоним `!обнулить рейтинг`).
- Звёздность: `Звёздность {ссылка}` (синонимы `!зв`, `звездность`), своя —
  `Моя звёздность`. Рейтинги: `Звёзды чата`, `Все звёзды`.
- Золотой рейтинг (по Ирис-голд, нужно открыть мешок командой `+Мешок`):
  `Золотой рейтинг`.
- Рейтинг коинов: вложить коины в чат — `Бкоин {число}`; топ чатов —
  `Бтоп` / за сегодня `Бтоп дня`; топ вкладчиков в чат — `Штоп`.

## 21. Награды

- Выдать медаль (степень 1–8, по умолчанию 1): `Наградить {степень}
  {ссылка} {enter} {описание}` (описание обязательно, перенос строки перед
  ним обязателен). С истечением срока: `Наградить {степень} удалить через
  {период} {enter} {описание}`. Ранг ограничивает максимально доступную
  степень.
- Управление награждающими: `+Награждающий {макс. степень} {ссылка}` /
  `-Награждающий {ссылка}`. Список: `Кто награждающий` (максимум 10 чел.).
- Снять: `Снять все награды {ссылка}`, `Снять награды от {ссылка}`,
  `Снять награду {номер} {ссылка}` (синоним `-награда {номер} {ссылка}`).
- Посмотреть: `Награды {ссылка}`, свои — `Мои награды`.

## 22. Закладки

Разметка обсуждений в чате для быстрого возврата к теме.

- Создать: `+Закладка {название} {enter} {текст/вложения}`, либо ответом на
  сообщение (закладка укажет на это место).
- Вызвать по номеру: `Закладка {номер}`.
- Список всех закладок чата (чатбук): `Чатбук` (постранично `Чатбук
  {номер страницы}`). Свои закладки: `Мои закладки` (постранично). Чужие:
  `Закладки {ссылка}` (постранично).
- Удалить свою/модератором: `Удалить закладку {номер}` (синоним
  `-закладка {номер}`). Убрать только из общего чатбука (без удаления у
  автора): `Исключить закладку {номер}` (синоним `убрать закладку`).
- Вернуть/исключить все закладки пользователя из чатбука: `+Кладмен
  {ссылка}` / `-Кладмен {ссылка}`.

## 23. Заметки

- Создать: `+Заметка {название} {enter} {описание}`. Удалить: `-Заметка
  {название/номер}`.
- Редактировать с сохранением разметки: `~Заметка {название/номер}
  {enter} {новое описание}`. Переименовать: `~Название заметки
  {название/номер} {enter} {новое название}`. Изменить только текст без
  разметки/медиа: `Текст заметки {название/номер} {enter} {новый текст}`.
- Список: `Заметки` (постранично).
- Лимиты: название до 40 символов, описание ~4000 символов; 100 заметок в
  обычном чате, 1000 — в премиум-чате. Ссылки/вложения в заметке стоят
  5 ирисок и проходят ручную модерацию.

## 24. Таймеры

Отложенные действия (от 1 минуты до 1 года). Доступны модераторам от 1
ранга и всем VIP. Лимит одновременных: 5 в обычном чате, 20 в премиум-чате.

- Через период: `Таймер через {период} {enter} {команда}`.
- На конкретную дату: `Таймер на {дд.мм.гг чч:мм} {enter} {текст/команда}`
  (время можно не указывать полностью).
- Список своих в чате: `Таймеры`. Удалить по номеру: `-Таймер {номер}`.
  Удалить все: `!Сбросить таймеры` (синоним `!удалить все таймеры`).

## 25. Каталог чатов

Публичный каталог на сайте iris-tg.ru/chats/. В каталог не берут чаты 18+,
с трэш-контентом, рейдерством или где ссылка ведёт на канал, а не чат;
нужно 35 000+ коинов на балансе чата (учитываются первые 100 подходящих
заявок).

- Подать заявку / обновить данные чата: `Каталог добавить` (описание —
  со следующей строки). Отменить заявку: `Каталог отменить`. Удалить чат
  из каталога: `Каталог удалить`.
- Категории (временно не работают): `Каталог категории`, привязать —
  `+Каталог категории {номера}`, отвязать — `-Каталог категории {номера}`.
- Показ модерации на сайте: `+Каталог модераторы` / `-Каталог модераторы`.
- Без команды `+Чат ссылка` вместо кнопки перехода в каталоге будет
  кнопка «Попроситься в чат».

## 26. Ирис-биржа (Ирис-голд)

Инвестиционный инструмент с плавающим курсом (спрос/предложение), прибыль
не гарантирована.

- Стакан заявок: `Биржа`. Свои заявки: `Биржа мои заявки`. Клавиатура с
  командами: `Биржа клава`.
- Купить по рынку: `Биржа купить по рынку {количество}`. Купить заявкой по
  своей цене: `Биржа купить {количество} {цена}`. Продать по рынку:
  `Биржа продать по рынку {количество}`. Продать заявкой: `Биржа продать
  {количество} {цена}`.
- График курса: `Биржа график` (за всё время) / `Биржа график {число
  дней}`.

## 27. Инлайн-режим

Использовать функции Ириса в любом чате/канале/личке, не добавляя бота —
достаточно ввести `@iris_cm_bot` в строке сообщения и продолжить фразу.

- Перевод ирисок собеседнику: `@iris_cm_bot передать {число} @` (или по
  `{ссылке}`); Ирис-голд — `@iris_cm_bot передать голд {число} @`; очки
  доната — `@iris_cm_bot передать од {число} @`; Telegram-звёзды —
  `@iris_cm_bot передать тгзв {число} @`. Без очков доната: `@iris_cm_bot
  передать {число} без од @` (аналогично для голда).
- Показать мешок собеседнику: `@iris_cm_bot мешок`. Создать чек: `@iris_cm_bot
  передать {кол-во}` (синоним `@iris_cm_bot создать чек {кол-во}`).
- Биржа: `@iris_cm_bot биржа` (текущий курс и стакан).
- РП/глобальные РП-команды: `@iris_cm_bot {название РП}`, с комментарием —
  `@iris_cm_bot {название РП} {enter} {текст}` (собеседник подтверждает
  кнопкой).
- Погода: `@iris_cm_bot погода {город}` (без города — из анкеты).

## 28. Бизнес-бот (л/с)

Ирис можно подключить как Telegram Business Bot для личных сообщений:
уведомляет о скам-аккаунтах/спамерах/подозрительных регистрациях, а также
доступны РП-команды, анкета собеседника, ID, погода и игровые команды.
Проверить подлинность: в чате с человеком сверху должна быть надпись «этим
чатом управляет бот» → «управление ботом» → должен открыться профиль
официального @iris_cm_bot; если бот другой — немедленно удалить его в
настройках.

## 29. Репорты (жалобы)

- Пожаловаться на сообщение (строго ответом на него, причина — следующей
  строкой): `!Репорт`.
- Настройка приёма жалоб: перенаправить жалобы из чата в отдельный админ-
  чат — `+Репорты из чата {код чата}`; из всей сетки — `+Репорты из
  сетки`; принимать жалобы прямо в этом чате — `+Репорты сюда`.
- Список необработанных жалоб: `Репортлист`. Очистить список: `!Сброс
  репортов`.

## 30. Розыгрыши

- Создать на N участников: `+Розыгрыш {кол-во участников}`. С публикацией
  в канал и итогами там же: `+Розыгрыш в канал {кол-во участников}
  @канал`.
- Статус текущего розыгрыша: `Розыгрыш статус`. Победители: `Победители
  розыгрыша`. Топ купивших билеты: `Розыгрыш топ билетов`.
- Режимы участия: «нажавшие ”Участвовать”», «все участники чата»,
  «недавно писавшие» (за период), «входной билет» (за ириски/голд),
  «аукцион удачи» (несколько билетов = выше шанс), «с VIP-статусом»,
  «звезда удачи» (шанс зависит от звёздности).
- Фильтры: по VIP-статусу, по минимальной звёздности, «без модераторов»
  (исключить назначенных в Ирисе модераторов).
- Прозрачность результатов — кнопка «Контроль честности» под итоговым
  сообщением.

## 31. Интеграция с Telegram (реакции)

Можно ограничивать/разрешать реакции для отдельных участников чата и
настраивать защиту от флуда реакциями (лимит реакций за 10 секунд + авто-
наказание через триггер).

## 32. Полезные «мета»-команды

- Список доступных в чате команд (с учётом ранга): `Команды` (синонимы
  `!помощь`, `/help`).
- Статистика семейства ботов Ирис: `!Актив Ириса`.
- Проверить, какой чат к какому привязан, узнать код: `Код чата`.

## Источники

Официальный список команд: teletype.in/@iris_cm/commands
Гайд по триггерам: teletype.in/@iris_cm/triggers
Про кланы и кружки: teletype.in/@iris_cm/clans_clubs
Про VIP: teletype.in/@iris_cm/vip · Про сетку чатов: teletype.in/@iris_cm/grid
Официальный канал: t.me/iris_cm · Короткие новости: t.me/iris_brief
Чат поддержки: t.me/iris_cm_chat · Официальный бот: @iris_cm_bot

> Примечание: у Iris очень много команд, и точные синонимы/параметры время
> от времени обновляются. Если в этой базе не нашлось точного ответа —
> предложи пользователю написать в чате команду «Команды» или заглянуть в
> официальный справочник по ссылке выше, вместо того чтобы придумывать
> несуществующий синтаксис.
