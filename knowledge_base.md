# База знаний по Iris | Чат-менеджер (@iris_cm_bot)

Это справочная база для бота-помощника «Ириска». Используй эти данные, чтобы
точно и по делу объяснять пользователю, какую команду Ириса написать и как
именно её оформить. Если вопрос не связан с ботом Iris — вежливо скажи, что
помогаешь только по Iris | Чат-менеджеру.

## Общие правила синтаксиса

- Префиксы команд: `!`, `.`, `/`, а также слова «Ирис» и «Ириска». Многие
  команды работают вообще без префикса.
- `{значение}` — обязательный параметр, который нужно подставить без скобок.
- `[вариант1|вариант2]` — нужно выбрать один из перечисленных вариантов.
- `{период}` — число + единица времени: минута/час/сутки/неделя/месяц/год.
  Например: `2 дня`, `1 неделя`, `10 минут`.
- `{ссылка}` — @username, ссылка на профиль (t.me/username) или ответ (reply)
  на сообщение нужного пользователя.
- Причину наказания (бана/мута/варна) принято писать на следующей строке,
  после самой команды.
- Enter/перенос строки в описании команд обозначается как `{enter}`.

## Модерация: баны, муты, кики, варны

- **Мут**: `Мут {срок} {ссылка}` — запрещает писать сообщения на указанный
  срок (по умолчанию 1 неделя). Синонимы: `заткнуть`, `mute`.
  Пример:
  ```
  Мут 7 дней @username
  Спам
  ```
  Снять мут: `-Мут {ссылка}` (синонимы: снять мут, говори, unmute, размут).
  Список замученных: `Муты`. Проверить: `Проверить мут {ссылка}`.
- **Бан**: `Бан {период} {ссылка}` — блокирует пользователя (без периода —
  бан навсегда). Синоним: `чс`. Разбан: `Разбан {ссылка}` (синоним: Вернуть).
  Причина бана: `Причина {ссылка}`. Список банов: `Банлист`.
  Снять все баны в чате: `!Амнистия`.
- **Кик**: `Кик {ссылка}` — исключает пользователя, но не мешает ему вернуться.
  Тихий кик без уведомления: `Кик тихо {ссылка}`.
- **Предупреждения (варны)**: `Варн {период} {ссылка}` — выдать
  предупреждение (синонимы: пред, предупреждение, warn). Снять последнее:
  `-Варн {ссылка}`. Все варны пользователя: `Варны {ссылка}`. Свои варны:
  `Мои варны`. Лимит варнов, после которого кикает: `Варны лимит {число}`
  (по умолчанию 3).
- Массовый кик неактивных: `Кик неактив {период}` (минимум 24 часа).
  Кик недавно вступивших (полезно при рейде): `Кик новичков {период}`.
  Кик активных за последнее время (при рейде): `Кик актив`.
  Кик удалённых аккаунтов: `Кик удалённых`.
- Ограничение доступа к любой команде по рангу модератора: `Дк {команда}
  {ранг}` (0 — всем, 6 — отключить).

## Ранги модерации (0–5)

0 — участник, 1 — младший модератор, 2 — старший модератор, 3 — младший
администратор, 4 — старший администратор, 5 — создатель.

- Назначить модератора: `+Модер {ссылка}` (для ранга 2+ — `+Модер 2 {ссылка}`
  и т.д.). Синоним: `Повысить {ссылка}` — на 1 ранг выше.
- Снять модератора: `Снять {ссылка}` (синоним: Разжаловать).
- Список модерации: `Кто админ` (синонимы: а судьи кто, кто здесь власть).
- Позвать модерацию: `Созвать модеров`.

## Чистка чата

- Удалить сообщения над командой: `-Смс {число}` (можно `тихо`).
- Удалить сообщения под отмеченным (ответом): `!Пург {число}` или `!Пург` —
  всё ниже. Нельзя удалять сообщения старше 48 часов.
- Лимиты: обычный чат — до 100 сообщений раз в 3 минуты; премиум — до 1000
  раз в минуту.

## Настройка чата

- Правила: `+Правила` (текст на следующей строке). Показать: `Правила`.
- Приветствие: `+Приветствие` (текст на следующей строке, поддерживает
  переменную `{имя}`).
- Ссылка на чат: `+Чат ссылка` / удалить `-Чат ссылка` / показать `Чат-ссылка`.
- Закрыть чат для обычных участников: `-Чат`, открыть: `+Чат`.
- Закрепить сообщение (ответом на него): `!Закреп`. Открепить: `!Открепить`.
- Минимальный возраст аккаунта в системе Ириса для новых участников:
  `+Минрег {число дней}`.
- Ограничение ссылок: `+Ссылки` / `-Ссылки` (все типы), либо по отдельности
  `+Сайты`, `+Чаты`, `+Теги`.
- Защита от рейда: `+Антирейд` — банит того, кто пригласил забаненного.
  `Инвайты {число}` — лимит приглашений одним участником.

## Анкета, профиль, статистика

- Профиль: `Кто я` (свой), `Кто ты {ссылка}` (чужой). Полная анкета:
  `Моя анкета`, `Анкета {ссылка}`.
- Ник в чате: `+Ник {текст}`, удалить `-Ник`.
- Топ активности: `Топ {число} [сутки|неделя|месяц|вся]`, например
  `Топ 10 неделя`.
- Кто сейчас в сети: `Кто онлайн`.
- Проверка ID: `!Ид {ссылка}`.

## Ириски, экономика, VIP

- «Ириски» — основная валюта Ириса, не привязана к чату. Передать:
  `Передать {число} {ссылка}`.
- Чек (резервирование и передача суммы по коду): `Создать чек {сумма}`,
  активировать `Активировать чек {код}`, отменить `Отменить чек {код}`.
- VIP-статус даёт дополнительные функции (свои РП-команды, эмодзи в нике и
  т.д.) — подробности предлагай смотреть в официальном канале.
- Ирис-голд и Ирис-биржа — внутренний инвестиционный инструмент с
  плавающим курсом, покупка/продажа через `Биржа купить/продать`.

## Развлечения и РП

- Пинг бота: `Пинг` (ответит «Понг»), полезно проверить, жив ли бот.
- Случайное число: `Рандом {число1} {число2}`.
- Да/нет: `!Данет {вопрос}`. Выбор: `!Выбери {первое} или {второе}`.
- Дуэль: `Дуэль {ссылка}`, согласиться `Дуэль да`, выстрел `Выстрел`.
- Кубы (быстрый аналог дуэли): `Кубы {ссылка}`, `Кубы да`.
- Погода: `!Погода {город}`.
- Шипперинг случайных/выбранных пользователей: `Шипперим` / `Шипперим
  {ссылка1} {ссылка2}`.
- РП-команды по образцу «Пожать руку {ссылка}» — большой набор действий,
  доступность зависит от возрастных настроек чата (0+/13+/18+).

## Отношения и браки

- Начать отношения: `+Отн {ссылка}`, принять `Отн да`, отклонить `Отн нет`,
  разорвать `-Отн {ссылка}`.
- Брак: `Брак {ссылка}` (предложение), принять `Брак да`, развод `!Развод`.
- Статистика браков: `Топ браков`, свой брак — `Мой брак`.

## Триггеры (автоматизация наказаний)

Триггер — правило вида «если участник сделал X — бот автоматически
выполняет команду Y» (например, автобан за мат или ссылку). Настройка
триггеров — отдельный расширенный раздел; для базовой автомодерации
пользователю обычно достаточно команд бана/мута/варнов выше, а для готовых
сценариев стоит посмотреть официальный гайд по триггерам в канале
поддержки Iris.

## Сетка чатов

Позволяет объединить несколько своих чатов и управлять ими одной командой
(«Сетка ...»): `Сетка баны`, `Сетка кик {ссылка}`, глобальный бан
`Глобан {ссылка}`, глобальный модератор `+Глмодер {ссылка}`.

## Привязка к личным сообщениям бота

`!Привязать` в чате (или `!Привязать {код чата}` в л/с бота, код можно
узнать командой `Код чата`) — открывает управление чатом прямо в личке с
Ирисом: настройки, наказания, статистика, доступ команд и т.д.

## Полезные «мета»-команды

- Список всех доступных в чате команд с учётом ранга: `Команды` или
  `!помощь` / `/help`.
- Кто назначил модератора: `Кто назначил {ссылка}`.
- Показать причину бана и кто его выдал: `Причина {ссылка}`.
- ID сообщения (ответом на него): `Смс ид`.

## Источники

Официальный список команд: teletype.in/@iris_cm/commands
Официальный канал: t.me/iris_cm · Чат поддержки: t.me/iris_cm_chat
Официальный бот: @iris_cm_bot

> Примечание: у Iris очень много команд и модулей (кланы, кружки, розыгрыши,
> репорты, каталог чатов, награды, закладки, заметки, таймеры и т.д.).
> Если в этой базе не нашлось точного ответа — предложи пользователю
> написать в чате команду «Команды» или заглянуть в официальный справочник
> по ссылке выше, вместо того чтобы придумывать несуществующий синтаксис.
